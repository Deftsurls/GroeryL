<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title>Add a new product to the list</title>
</head>
<body style="background-image: url('/nue.png'); background-position: center; background-size: cover; background-repeat: no-repeat; height: 100vh; margin: 0;">
    <h1 class="display-3 text-center position-relative mt-3 mb-3" style="font-family: Arial, Helvetica, sans-serif;">Please enter a new product</h1>
    <div class="col-6 offset-3">
        <form action="/products" method="post"  id="form">
            <div class="mb-3">
                <label class="form-label" for="name"><b>Product Name:</b></label>
                <input class="form-control" type="text" id="name" name="name" required>
            </div>
            <br>
            <div class="mb-3">
                <label for="quantity" class="form-label"><b>Quantity:</b></label>
                <input type="number" class="form-control" id="quantity" min="1" name="quantity" required>
            </div>
            <br>
            <div class="mb-3">
                <label for="unit" ><b>Unit:</b></label>
                <select name="unit" id="" class="form-select">
                <% for(let unit of units){ %>
                    <option value="<%=unit%>"><%=unit%></option>
                <% } %>
                </select>
            </div>
            <br>
            <div class="mb-3">
                <label for="price" class="form-label"><b>Price $:</b></label>
                <div class="input-group">
                    <span class="input-group-text" id="price-label">$</span>
                    <input type="text" class="form-control" type="number" id="price" placeholder="0.000" name="price" aria-label="price" 
                    aria-describedby="price-label">
                </div>
            </div>
            <script>
                const priceInput = document.getElementById('price');
              
                priceInput.addEventListener('input', (e) => {
                  const value = e.target.value.replace(/\D+/g, ''); // Remove any non-numeric characters
                  const formattedValue = value.replace(/(\d)(?=(\d{3})+$)/g, '$1.'); // Add a point as a thousand separator
                  e.target.value = formattedValue;
                });
              
                const form = document.getElementById('form'); // Replace with your form ID
                form.addEventListener('submit', (e) => {
                  const priceValue = priceInput.value.replace(/\./g, ''); // Remove the thousand separator point
                  priceInput.value = priceValue; // Update the input value without the point
                });
              </script>
            <div class="d-flex justify-content-center pt-2">
            <button type="submit" class="btn btn-success">Add Product</button>
            <a href="/groceriesList" class="btn btn-danger" style="left: auto;margin-left: 20px;">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>